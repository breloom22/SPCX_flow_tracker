# .env.example — 복사해서 .env 로 사용 (.env는 커밋 금지, .gitignore 처리됨)

# Telegram 알림 (선택). 미설정 시 알림은 no-op.
# 1) @BotFather 로 봇 생성 → 토큰
# 2) 봇과 대화 시작 후 chat id 확인 (예: https://api.telegram.org/bot<token>/getUpdates)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
