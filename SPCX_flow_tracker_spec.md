# SPCX Mechanical Flow Tracker — Claude Code 빌드 지시문

> 이 문서는 Claude Code에게 전달하는 프로젝트 스펙이다. 처음부터 끝까지 읽고, Phase 순서대로 구현하라.
> 프로젝트 루트에 이 파일을 `SPEC.md`로 두고, 구현 결정 사항은 `DECISIONS.md`에 기록하라.

---

## 0. 한 줄 목표

**"기계적으로 움직일 수밖에 없는 자금(패시브 인덱스, 락업 매도, VC/펀드 분배)의 시점과 규모를 사전에 정량화하고, 그 이벤트 윈도우 주변의 비정상 플로우 신호로 재량적 자금(스마트머니)의 선행 움직임을 추정한다."**

대상: SpaceX(SPCX) IPO. 2026-06-11 공모가 확정, 2026-06-12 나스닥 상장 예정.
시스템은 SPCX 특화로 시작하되, 다른 대형 IPO(OpenAI, Anthropic 등 후속 상장 가능성)에 재사용 가능하도록 이벤트/티커를 파라미터화하라.

## 1. 설계 철학 (모든 모듈에 적용)

1. **확정적(deterministic) 플로우와 추론(inferred) 신호를 코드 레벨에서 분리하라.** 인덱스 편입 매수는 계산이고, 헤지펀드 선행 매매는 추정이다. 모든 출력에 `confidence: deterministic | rule_based | inferred` 태그를 붙인다.
2. **포지션이 아니라 플로우를 추적한다.** 기관의 실시간 포지션은 관측 불가. 관측 가능한 것은 공시, ETF 설정/환매, 거래량/대차/옵션 등 '그림자'뿐이다. 시스템은 그림자에서 역추정한다.
3. **모든 수치에 출처·시점·지연(latency)을 메타데이터로 저장하라.** 13F는 45일 지연, 공매도 잔고는 격주, ATS 데이터는 2~4주 지연이다. 지연을 모르는 신호는 신호가 아니다.
4. **무료 소스 우선, 유료 소스는 어댑터로.** 데이터 레이어는 인터페이스(추상 클래스)로 추상화하고, Polygon/유료 옵션 데이터 등은 나중에 어댑터만 추가하면 되게 하라.
5. **매매 시그널 생성기가 아니다.** 출력은 "언제, 얼마의 기계적 자금이 움직이는가 + 그 전에 비정상 신호가 있었는가"라는 정량 리포트다. 추천/주문 기능은 만들지 마라.

## 2. 도메인 컨텍스트 (하드코딩 가능한 사실, `config/spcx.yaml`에 저장)

아래는 2026-06-10 기준 보도된 사실이다. 구현 시 최신 공시(EDGAR의 424B4)로 검증·갱신하라.

- 티커 SPCX, 나스닥. 공모가 목표 $135/주, 5억 5,660만 주, 조달 ~$750억, 밸류에이션 ~$1.75조.
- 수요 약 2배 초과청약(~$1,500억). 공모 물량의 ~30%(~$225억)가 리테일 배정 (Robinhood, Fidelity, Schwab, SoFi, E*Trade 경유).
- 초기 유통물량(free float) ~$750억 수준으로 보도됨.
- **나스닥100 패스트 엔트리**: 2026-05-01 발효 규정 — 시총 상위 40위 신규 상장사는 상장 후 15거래일 만에 편입 가능. 최소 유통주식 요건 폐지. 저유통 종목에 최대 3x float 가중치 멀티플라이어 적용 (유통 시총 $750억 → 가중치 계산용 $2,250억 가능성).
- **S&P 500**: 2026-06-04 S&P가 자체 패스트 엔트리 제안을 부결. SPY/S&P500 연계 펀드의 강제 매수는 2027년까지 사실상 없음.
- **락업 구조 (단계적/staggered)**: 머스크 366일 매도 금지. 기타 내부자는 **첫 분기 실적 발표(8월 예상) 후 둘째 거래일**부터 일부 매도 가능. 성과/이벤트 기반 조기 해제 조항 존재 → 424B4에서 정확한 트랜치 구조를 파싱해서 캘린더화하는 것이 Phase 1 핵심 과제.
- 알려진 프리IPO 보유자(분배/매도 압력 후보): Alphabet (~7% 보도), Fidelity, DXYZ(폐쇄형 펀드, 매도 의무 없음), XOVR(SPV 구조), Fundrise VCX(영구자본, 매도 의무 없음), 다수 VC 성장펀드(LP 자본 반환 의무 → 구조적 매도자).
- 보유자 분류 체계: `forced_seller`(만기 있는 VC 펀드) / `discretionary`(영구자본, 전략적 보유자) / `unknown`.

## 3. 시스템 아키텍처

```
spcx-flow-tracker/
├── SPEC.md                  # 이 문서
├── DECISIONS.md             # 구현 결정 로그
├── config/
│   ├── spcx.yaml            # 위 도메인 사실 + 이벤트 파라미터
│   └── sources.yaml         # 데이터 소스별 URL, 지연, 갱신주기
├── src/
│   ├── ingest/              # 데이터 수집 어댑터 (소스당 1모듈)
│   ├── mechanical/          # Module A: 확정적 플로우 엔진
│   ├── observe/             # Module B: 플로우 관측 레이어
│   ├── infer/               # Module C: 비정상 신호 / 추론 엔진
│   ├── calendar/            # Module D: 이벤트 캘린더 + 알림
│   └── report/              # Module E: 일일 리포트 / 대시보드
├── data/                    # DuckDB 파일 + raw 캐시 (gitignore)
└── tests/
```

스택: **Python 3.12, DuckDB(저장), pandas/polars, httpx, APScheduler 또는 cron, 리포트는 우선 Markdown/CLI → 여유 되면 Streamlit.** 과한 인프라(에어플로우, k8s) 금지. 단일 머신에서 `make daily` 한 번으로 전체 파이프라인이 돌게 하라.

---

### Module A — Mechanical Flow Engine (확정적 플로우)

**A1. 인덱스 편입 계산기 (`mechanical/index_flows.py`)**
- 입력: SPCX 시총(일별 종가 기준), float 추정치, 나스닥100 가중치 규칙(3x 멀티플라이어 포함), 나스닥100 추종 AUM 추정(QQQ + QQQM + 연계 뮤추얼펀드/연금; QQQ AUM은 공식 사이트에서 일별 수집).
- 출력:
  - `inclusion_date_estimate`: 상장일 + 15거래일 (미국 거래일 캘린더 적용 → 7월 초). 나스닥 공식 발표가 나오면 발표값으로 교체.
  - `forced_buy_usd`: 추정 편입 가중치 × 추종 AUM.
  - `funded_sell_by_constituent`: 비례 재조정 가정 하에 기존 나스닥100 종목별 예상 매도 금액 테이블 (상위 비중 종목일수록 큼). 이 테이블이 C 모듈의 "선행 매매 탐지" 대상 종목 리스트가 된다.
  - 민감도 분석: 주가 ±20%, float ±30%, 멀티플라이어 1x/2x/3x 시나리오 매트릭스.
- **자기강화 루프 시뮬레이터**: 편입 매수 → 시총 상승 → 가중치 상승 → 추가 매수 루프를 간단한 반복 모델로 구현하고, float 대비 누적 패시브 수요 비율을 출력하라 (가격 임팩트 모델은 단순 선형 가정으로 시작, 정교화는 나중에).

**A2. 락업 캘린더 (`mechanical/lockup.py`)**
- EDGAR에서 SPCX의 424B4(최종 prospectus)를 받아 락업 섹션을 파싱하라 (EDGAR full-text search API + 문서 다운로드, User-Agent 헤더 필수).
- 트랜치별로 `{해제 조건, 트리거(실적일 기준 상대 날짜 or 절대 날짜 or 주가 조건), 해제 가능 주식 수, 보유자 그룹}`을 구조화.
- 첫 분기 실적일은 미정이므로 `earnings_date` 변수로 두고, 보도 기준 추정치(8월)를 기본값으로. 실적일 확정 시 캘린더 전체가 자동 재계산되게 하라.
- 출력: 날짜별 누적 매도 가능 물량(주식 수, USD, float 대비 %) 시계열.

**A3. 보유자/분배 트래커 (`mechanical/holders.py`)**
- 시드 데이터: config의 알려진 보유자 리스트.
- 상장 후: 13F(분기), N-PORT(펀드 월별 보유, 60일 지연), Form 4/144(내부자 매도, 준실시간 — 이게 가장 빠른 확정 신호) 를 EDGAR에서 수집.
- DXYZ는 폐쇄형 펀드라 일별 시장가격과 (공시되는) NAV의 괴리를 계산 가능 → **DXYZ 프리미엄/디스카운트를 리테일 과열 게이지로 별도 시계열화** (C 모듈에서 사용).
- 보유자별 `forced_seller` 여부에 따라 "구조적 매도 압력" 추정치를 락업 캘린더에 합산.

### Module B — Flow Observation Layer (관측)

소스별 어댑터. 각 어댑터는 `fetch() → normalize() → upsert(duckdb)` 패턴 통일.

| 시리즈 | 소스 | 주기/지연 | 비고 |
|---|---|---|---|
| QQQ 등 ETF 일별 보유내역/AUM | 운용사 공식 CSV (Invesco 등) | 일별 | 보유내역 diff로 리밸런스 실집행 감지 |
| ETF 설정/환매 (QQQ, 우주테마 UFO·NASA·XOVR·DXYZ, BTC 현물 ETF IBIT·FBTC 등) | 운용사 공시 + shares outstanding 변화 | 일별 | BTC ETF 유출은 크로스에셋 유동성 흡수 프록시 |
| SPCX/관련 종목 OHLCV·거래량 | yfinance (시작), 어댑터로 교체 가능 | 일별/지연시세 | |
| 공매도 잔고 | FINRA short interest | 격주, ~1주 지연 | |
| 대차 수수료(borrow fee) | IBKR API 보유 시 사용, 없으면 스킵하고 인터페이스만 | 일별 | 선택 |
| 다크풀/ATS 체결 비중 | FINRA OTC Transparency 데이터 | 주별, 2~4주 지연 | |
| 옵션 OI / IV / 풋콜비 (SPCX 옵션 상장 후) | CBOE 지연 데이터 or yfinance options | 일별 | 딜러 감마 추정은 v2, 우선 OI·스큐만 |
| MMF 자금 플로우 | ICI 주간 통계 | 주별 | IPO 청약 자금 이동 프록시 |
| 마진 부채 | FINRA margin statistics | 월별 | |
| 내부자 매도 | EDGAR Form 4/144 (A3와 공유) | 준실시간 | 락업 해제 후 핵심 |

원칙: 스크래핑이 깨지기 쉬운 소스는 raw 응답을 `data/raw/`에 날짜별로 그대로 캐시한 뒤 파싱하라 (재파싱 가능하게).

### Module C — Anomaly / Inference Engine (추론)

핵심 질문: **"확정 이벤트(A 모듈 캘린더) 이전에, 누군가 미리 움직인 흔적이 있는가?"**

1. **베이스라인 + z-score**: 각 관측 시리즈에 대해 롤링 윈도우(60일 권장, 시리즈 주기에 맞게) 평균/표준편차 기반 z-score. |z| ≥ 2를 '비정상'으로 플래그.
2. **이벤트 스터디 프레임**: A 모듈의 각 이벤트(편입일, 락업 해제일, 실적일)에 대해 [T-15, T+5] 윈도우를 정의하고, 윈도우 내 비정상 플래그를 이벤트에 귀속시켜 저장. 사후에 "이벤트 며칠 전부터 신호가 나왔는가" 통계를 자동 집계.
3. **편입 선행 매매 탐지**: A1의 `funded_sell_by_constituent` 상위 종목들에 대해, 나스닥100 동일가중 바스켓 대비 상대수익률을 일별 계산. 편입 발표 전 유의한 상대 약세 = 선행 매도 추정 신호 (`inferred` 태그).
4. **리테일 과열 게이지**: DXYZ 프리미엄, 우주테마 ETF 유입, SPCX 거래량 중 소액주문 비중(가능하면) 합성 지수.
5. **크로스에셋 흡수 지수**: BTC ETF 순유출 + MMF 유입/유출 + QQQ 환매를 결합한 단순 합성 점수 — "IPO가 주변 유동성을 빨아들이는 강도".
6. 통계 기법은 단순하게 시작하라. 머신러닝 금지(Phase 3 전까지). 해석 가능성이 우선이다.

### Module D — Event Calendar & Alerts

- 모든 확정/추정 이벤트를 단일 테이블로: `{date, event_type, magnitude_usd, confidence, source, notes}`.
- 초기 이벤트 목록: 2026-06-11 프라이싱, 06-12 상장, 옵션 상장일(거래소 발표 모니터링), 상장+15거래일 나스닥100 편입 추정일, 나스닥 공식 편입 발표, Q2 실적일(추정 8월), 실적+2거래일 락업 1차 해제, 이후 트랜치들, 13F 공개일(8월 중순: 6/30 기준 포지션 확인 가능), S&P 지수 정기 리뷰 일정(2027 편입 시나리오용).
- 알림: 우선 일일 리포트에 "D-N 카운트다운" 섹션으로 충분. 웹훅/텔레그램은 인터페이스만 만들어 두라.

### Module E — Daily Report

`make daily` 실행 시 `reports/YYYY-MM-DD.md` 생성:

1. **카운트다운**: 다가오는 이벤트 D-N, 각 이벤트의 기계적 자금 규모 추정.
2. **기계적 플로우 현황**: 편입 매수 추정치(시총 변동 반영 갱신), 락업 누적 해제 물량, 구조적 매도자 잔여 추정.
3. **관측 요약**: 주요 시리즈 최신값 + z-score, 신규 비정상 플래그.
4. **추론 섹션**: 선행 매매 신호, 리테일 과열 게이지, 크로스에셋 흡수 지수 — 모두 `inferred` 명시.
5. **데이터 신선도 테이블**: 시리즈별 마지막 갱신일과 알려진 지연. 오래된 데이터로 결론 내리는 것을 구조적으로 방지.

---

## 4. 구현 순서 (상장이 6/12라 시간이 없다 — 이 순서 엄수)

**Phase 1 (즉시, 상장 전 완성 목표)**
1. 리포 골격 + config + DuckDB 스키마 + 이벤트 캘린더(D) 하드코딩 버전.
2. A1 인덱스 편입 계산기 (민감도 매트릭스 포함) — 가장 가치 높고 외부 의존성 낮음.
3. EDGAR 어댑터 + A2 락업 캘린더 — 424B4 원문을 inbox에 캐시하고, **락업 조항 추출은 Claude Code가 직접 수행**(섹션 5), 파이프라인은 extracted YAML을 검증·적재해 캘린더 생성.
4. 일일 리포트(E) 최소 버전.

**Phase 2 (상장 첫 2주)**
5. B 관측 어댑터: yfinance 시세, QQQ 보유내역 diff, ETF shares outstanding, Form 4/144 watcher.
6. C의 z-score + 편입 선행 매매 탐지 + DXYZ 게이지.
7. 나스닥100 편입 발표 모니터링 (나스닥 보도자료 페이지 폴링).

**Phase 3 (편입 이후 ~ 8월 락업)**
8. 옵션 데이터, 공매도/ATS, 13F 파서, 이벤트 스터디 자동 집계.
9. 첫 락업 해제(8월) 전후 데이터로 시스템 첫 실전 검증 → 결과를 DECISIONS.md에 기록.

## 5. LLM 레이어 — 별도 API 호출 없이 Claude Code 자신이 수행한다

**원칙: 파이프라인 코드에는 LLM API 호출을 넣지 않는다.** 비정형 텍스트 → 구조화 데이터 변환은 Claude Code(이 스펙을 읽고 있는 너 자신)가 운영 루틴의 일부로 직접 수행한다. 파이프라인은 100% 결정적 코드로 유지된다.

**인수인계 프로토콜:**
- 파이프라인이 새 비정형 문서를 발견하면(EDGAR 신규 공시, 보도자료 등) 원문을 `data/inbox/`에 캐시하고, 일일 리포트에 "미처리 문서 N건" 섹션으로 표시한다. 해당 문서에 의존하는 시리즈는 stale 처리.
- Claude Code는 운영 세션에서 inbox를 읽고, `src/schemas.py`의 Pydantic 모델에 맞는 YAML을 `data/extracted/`에 작성한다. 각 파일에 반드시 포함: 출처(액세션 번호/URL), 추출 일시, 신뢰도, **근거가 된 원문 문장 인용**.
- 파이프라인은 extracted 파일을 Pydantic으로 재검증한 뒤에만 DB에 적재한다. 검증 실패 시 적재 거부 + 리포트에 사유 표시.
- 모호하거나 해석이 갈리는 조항은 임의로 해석하지 말고 `needs_review: true`로 표시한 뒤 사용자에게 질문으로 올린다.

**Claude Code 담당 추출 작업:**
1. **424B4 락업 조항 → 트랜치 스키마** (Phase 1에서 즉시 1회 수행, 정정 공시 나오면 재수행). 조건부 트랜치(실적일 상대 날짜, 주가 조건, 보유자 그룹별 비율)를 빠짐없이 구조화. 이것이 A2 모듈의 입력이 된다.
2. **Form 4/144 각주 해석** — 10b5-1 사전 계획 매도인지 재량 매도인지 분류 (의미가 완전히 다르므로 핵심).
3. **나스닥/지수 제공사 보도자료** → 편입일, 실적일 등 이벤트 확정값 추출 → 이벤트 캘린더 갱신.
4. (선택) 일일 리포트 말미의 서술 요약.

**일일 운영 루틴 (CLAUDE.md에 기록하고 매 세션 이 순서로):**
1. `make daily` 실행 → 수집 + 계산 + 리포트 생성
2. 리포트의 "미처리 문서" 섹션 확인 → inbox 처리 → extracted 작성
3. `make daily` 재실행 → 최종 리포트 확인 → 특이사항을 사용자에게 보고

**금지:** 수치 계산을 추론으로 대체하지 마라. 추출은 원문에 명시된 것만, 계산은 코드로만. 원문에 없는 값을 "합리적 추정"으로 채우지 마라 — 빈 값 + needs_review가 정답이다.

**트레이드오프 인지:** 이 구조는 완전 무인 자동화가 아니다. 추출 지연의 하한 = Claude Code 세션 주기(일 1회). 일별 데이터 시스템이므로 허용 가능하나, 락업 해제 주간(8월)에는 세션을 하루 2회로 늘리는 것을 사용자에게 제안하라.

## 6. 품질 기준

- 모든 어댑터에 단위 테스트(고정 fixture 기반 파싱 테스트). 네트워크 호출은 테스트에서 mock.
- 파이프라인은 멱등(idempotent): 같은 날 두 번 돌려도 중복 없음.
- 외부 소스 실패 시 전체 파이프라인이 죽지 않고 해당 시리즈만 stale 표시.
- 모든 추정치 함수에 가정(assumptions)을 docstring과 리포트 각주로 명시.
- 비밀키/API키는 `.env`, 커밋 금지.

## 7. 명시적 비목표 (만들지 마라)

- 주문 집행, 매매 추천, 백테스트 기반 전략 시뮬레이터 (분석 시스템이지 트레이딩 봇이 아님).
- 실시간 tick 데이터 처리 (일별/일중 스냅샷으로 충분).
- ML 모델 (Phase 3 이후 별도 논의).
- **파이프라인 내 LLM API 호출** (섹션 5의 원칙 — 추출은 Claude Code 세션이 수행).

## 8. 시작 절차

1. 이 SPEC을 읽고, 불명확한 가정 5개 이내를 질문으로 정리해 사용자에게 확인받아라 (특히: IBKR 등 보유 중인 데이터 계정, 실행 환경, 알림 채널).
2. CLAUDE.md를 생성하고 섹션 5의 일일 운영 루틴을 기록하라.
3. 확인 후 Phase 1을 순서대로 구현하라. A2 단계에서는 EDGAR에서 SPCX 424B4를 직접 받아 **너 자신이 락업 조항을 추출**하고(섹션 5 프로토콜), 그 결과로 락업 캘린더를 생성하라.
4. 각 단계 완료 시 실제 출력 샘플(편입 계산 매트릭스, 락업 캘린더, 일일 리포트)을 보여줘라.
